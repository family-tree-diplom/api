get Request and return Response object here. Response object is used by 3-d party features. Never return Domain entity outside!

описание сервисов, которые доступны для сторонних модулей (фич). Здесь на вход принимается ResponseDTO и возвращается RequestDTO

содержит классы, реализующие бизнес-логику приложения
